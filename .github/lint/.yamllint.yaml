---
extends: default

ignore: |
  *.sops.*
  gotk-components.yaml

rules:
  comments:
    min-spaces-from-content: 1
  indentation:
    spaces: 2
  line-length: disable
  truthy:
    ignore: |
      .github/workflows
